name: finance_domain
version: 1.0.0
config-version: 2

profile: finance_domain

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  finance_domain:
    input:
      +schema: input
      +materialized: table
    output:
      +schema: output
      +materialized: table
      +tags: ["data_product"]
